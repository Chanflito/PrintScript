#!/bin/bash

echo "Running 'gradle build' before committing..."

./gradlew build

# Run 'gradle build' and check the exit code
if ./gradlew build; then
    echo "Gradle build successful. Proceeding with commit."
    exit 0
else
    echo "Gradle build failed. Please fix the issues and try again."
    exit 1
fi
